## 2026-03-27 - US-009
- Created `lib/features/payment/checkout_screen.dart`: summary card (bid + platform fee + total), escrow note, "Pay S$X.XX" button
- Created `lib/features/payment/payment_service.dart`: wraps flutter_stripe + Cloud Functions (region: asia-southeast1); createEscrowPayment() → clientSecret; presentPaymentSheet() → bool/throws
- Added `paymentServiceProvider` (simple Provider)
- Added checkout GoRoute to `app_router.dart`; passes all data via state.extra Map
- Initialised `Stripe.publishableKey` in `main.dart` (test key placeholder)
- Added flutter_stripe ^11.1.0 + cloud_functions ^5.1.3 to pubspec.yaml
- Files changed: `lib/features/payment/checkout_screen.dart` (new), `lib/features/payment/payment_service.dart` (new), `lib/core/router/app_router.dart`, `lib/main.dart`, `pubspec.yaml`, `pubspec.lock`
- **Learnings for future iterations:**
  - CheckoutScreen receives all data via GoRouter `state.extra as Map<String, dynamic>` — no path params needed for complex objects
  - `AppRoutes.checkout = '/checkout/:taskId'`: navigate with `context.push(AppRoutes.checkout.replaceFirst(':taskId', id), extra: {...})`
  - Cloud Function region must match deployment — hardcoded to `asia-southeast1`
  - `StripeException` with `FailureCode.Canceled` distinguishes user cancel from real errors
  - Platform fee constant lives in `AppConstants.platformFeePercent`
---

## 2026-03-27 - US-008
- Chat feature (ChatListScreen, ChatThreadScreen, ChatRepository, ChatModel, MessageModel) was already committed to main in an earlier "complete core features" commit
- Sprint 2 integration: `_MessageButton` in TaskDetailScreen calls `chatRepository.createOrGetChat()` then navigates to `/chats/:chatId`
- `AppRoutes.chatThread = '/chats/:chatId'` already registered in router
- No new files needed for US-008; integration lives in task_detail_screen.dart (US-006 commit)
- **Learnings for future iterations:**
  - `createOrGetChat(taskId, posterId, providerId)` uses deterministic chatId from sorted UIDs — idempotent, safe to call multiple times
  - Chat path in Firestore: `chats/{chatId}/messages/{messageId}`
  - `buildChatId(taskId, uid1, uid2)` sorts UIDs lexicographically for stable IDs
  - Image messages: upload to `chat_media/{chatId}/{uuid}.{ext}` in Firebase Storage first, then send message with imageUrl
---

## 2026-03-27 - US-007
- Updated `lib/features/bids/presentation/widgets/bid_list_section.dart`: BidListSection + _BidCard with accept/reject buttons; accept calls `acceptBid()` then pushes to CheckoutScreen
- Updated `lib/features/bids/presentation/widgets/submit_bid_sheet.dart`: form with amount, duration chips (_BidDuration enum: 1h/2h/half-day/full-day/custom), 200-char proposal textarea
- Fixed pre-existing unused import warnings in profile_setup_screen.dart, role_selection_screen.dart, photo_gallery_screen.dart
- Files changed: `bid_list_section.dart`, `submit_bid_sheet.dart`, 3 screens with import cleanup
- **Learnings for future iterations:**
  - `acceptBid()` is a batch write: sets accepted bid → accepted, all other pending bids → rejected, task → assigned
  - `bidsStreamProvider` is a `StreamProvider.family<List<BidModel>, String>` keyed on taskId
  - Duration chips use a local enum `_BidDuration` with `.minutes` and `.label` extensions — BidModel has no duration field yet
---

## 2026-03-27 - US-006
- Rewrote `lib/features/tasks/presentation/screens/task_detail_screen.dart` (613 lines)
- _PhotoCarousel: horizontal ListView of CachedNetworkImage
- _PosterInfo: avatar + "Posted by" row
- Poster view: _MarkCompleteButton (AlertDialog confirm → completeTask()), _MessageButton (createOrGetChat → chatThread), BidListSection
- Provider view: Submit Bid bottom sheet, _ProviderBidView (own bid status card), _MessageButton, locked state
- _StatusBadge for bid status (pending/accepted/rejected)
- Fixed: removed unused bid_repository import, removed unnecessary ! on currentUser.uid (Dart flow-promotes inside isProvider branch)
- Files changed: `lib/features/tasks/presentation/screens/task_detail_screen.dart`
- **Learnings for future iterations:**
  - `isProvider = !isPoster && currentUser != null` — Dart promotes currentUser to non-null inside `else if (isProvider)` blocks, do NOT add `!`
  - `BidListSection` consumes `bidsStreamProvider` internally — just pass taskId
  - `showModalBottomSheet` needs `isScrollControlled: true` for forms with keyboard
  - `taskDetailProvider` is a `StreamProvider.family<TaskModel?, String>` keyed on taskId
---

## 2026-03-21 - US-005
- Created `lib/features/profile/presentation/screens/profile_screen.dart`
- `ProfileScreen` shows header (avatar, name, phone), My Role section, quick-links (Edit Profile, Verification Centre, Photo Gallery, Sign Out)
- `_RoleSection` shows current role label via `UserRole.displayLabel` extension and a 'Change Role' TextButton
- `_RolePickerSheet` bottom sheet with animated role cards matching `RoleSelectionScreen` style; calls `authRepository.createOrUpdateUser` and shows 'Role updated!' SnackBar
- Updated `app_router.dart`: replaced `_ProfileTab` stub with `ProfileScreen` for `AppRoutes.myProfile`
- Home shell auto-rerenders because `HomeScreen` watches `currentUserProvider` (Firestore stream)
- Files changed: `lib/features/profile/presentation/screens/profile_screen.dart` (new), `lib/core/router/app_router.dart`
- **Learnings for future iterations:**
  - `_ProfileTab` stub in `app_router.dart` is the pattern for unimplemented tabs — replace stub with real screen + import
  - `showModalBottomSheet` with `backgroundColor: Colors.transparent` + `Container` with `borderRadius` gives the floating sheet look
  - Bottom sheet context is different from scaffold context — `ScaffoldMessenger.of(context)` must use the *parent* scaffold's context (passed from `ConsumerStatefulWidget`), not the sheet's own context; use `Navigator.pop(context)` then show snackbar on original context
  - `UserRole` extension methods are the right pattern for display labels (keeps logic out of UI widgets)
---

## 2026-03-21 - US-004
- Created `lib/features/profile/presentation/widgets/provider_stats_widget.dart`
- `ProviderStatsWidget` is a `StatefulWidget` with a show/hide toggle for earnings
- Three-column card: Completed Tasks | Rating (with review count suffix) | Earnings (masked)
- Earnings defaults to '••••••'; tapping the cell toggles visibility and swaps the eye icon
- If `stats` is null, all cells show '--' placeholder
- Inserted widget as `SliverToBoxAdapter` in `ProviderHomeScreen` between SliverAppBar and category chips
- Files changed: `lib/features/profile/presentation/widgets/provider_stats_widget.dart` (new), `lib/features/home/presentation/screens/provider_home_screen.dart`
- **Learnings for future iterations:**
  - `ProviderStats` lives inside `UserModel.stats` (nullable) — always guard with `?.`
  - `earningsTotal` field name in `ProviderStats` (not `earnings` or `totalEarnings`)
  - Widget lives in `lib/features/profile/presentation/widgets/` — profile feature owns provider-specific UI atoms
  - Used `StatefulWidget` (not `ConsumerWidget`) since the toggle is purely local UI state — no Riverpod needed
---

## Codebase Patterns
- Use `timeago` package (already imported in task_list_screen.dart) for human-readable timestamps
- `AppColors`, `AppRadius`, `AppSpacing` are the design token classes in `lib/core/theme/app_theme.dart`
- `AppRoutes` constants are in `lib/core/constants/app_constants.dart`
- `AppCategories.all` is a `List<ServiceCategory>` with id, label, emoji, color fields; use `AppCategories.getById(id)` for safe lookup
- Providers use Riverpod: `StreamProvider`, `StateNotifierProvider`, `Provider` patterns
- `currentUserProvider` gives `AsyncValue<UserModel?>` via stream from Firestore
- `taskRepositoryProvider` exposes `watchMyPostedTasks(uid)` (all poster tasks) and `watchOpenTasks()` (open tasks)
- `TaskModel.budgetDisplay` extension gives formatted "S$X–S$Y" budget string
- Use `context.push()` for sub-routes, `context.go()` for main navigation tabs
- `flutter analyze` passes with no errors (warnings/infos from pre-existing code are OK)
- `withOpacity` is deprecated in this Flutter version; `.withValues(alpha: x)` is preferred, but existing code uses `withOpacity` throughout

# Ralph Progress Log
Started: Sat Mar 21 17:35:11 +08 2026
---

## 2026-03-21 - US-001
- Created `lib/features/home/presentation/screens/poster_home_screen.dart`
- Added `_posterActiveTasksProvider` (StreamProvider.autoDispose) that watches user's posted tasks and filters out completed/cancelled client-side
- `PosterHomeScreen` with SliverAppBar greeting, Post a Task ElevatedButton CTA, and SliverList of task cards
- `_PosterTaskCard` shows title, category emoji (via AppCategories.all lookup), budget display, bid count, and timeago timestamp
- Empty state shows '📋 No active tasks. Post your first task!'
- Files changed: `lib/features/home/presentation/screens/poster_home_screen.dart` (new)
- **Learnings for future iterations:**
  - `watchMyPostedTasks` in TaskRepository returns all poster tasks — filter status client-side in the provider
  - `AppCategories.all.where((c) => c.id == task.categoryId).firstOrNull` is the pattern to look up a category
  - `TaskModelExt` extension is in `task_model.dart` — `budgetDisplay`, `urgencyDisplay` are available
  - No existing `task_list_provider` method filters by poster+active; created inline StreamProvider instead
---

## 2026-03-21 - US-002
- Created `lib/features/home/presentation/screens/provider_home_screen.dart`
- Added `_selectedCategoryProvider` (StateProvider.autoDispose) for category filter state
- Added `_openTasksProvider` (StreamProvider.autoDispose) calling `watchOpenTasks(categoryId, limit: 20)`
- `ProviderHomeScreen` with SliverAppBar greeting + avgRating stars (if stats exist)
- `_CategoryFilterRow` horizontal scrollable FilterChip list from user's serviceCategories; "All" chip clears filter
- `_ProviderTaskCard` shows emoji, title, category chip, budget chip, neighbourhood, and timeago; taps navigate to `/tasks/:id`
- Empty state shows '🔍 No open tasks right now. Check back soon!'
- Files changed: `lib/features/home/presentation/screens/provider_home_screen.dart` (new)
- **Learnings for future iterations:**
  - `AppCategories.getById(id)` is the cleaner helper vs `.where().firstOrNull` pattern
  - `watchOpenTasks` accepts optional `categoryId` and `limit` params — pass null to get all
  - `StateProvider.autoDispose` is appropriate for ephemeral UI state like filter selection
  - `FilterChip` is the standard Flutter widget for toggleable category chips
  - `task.neighbourhood` is nullable — guard with `!= null && isNotEmpty` check
---

## 2026-03-21 - US-003
- Updated `HomeScreen` in `main_shell_screen.dart` to be role-aware
- Converted `HomeScreen` from `ConsumerWidget` to `ConsumerStatefulWidget` to hold a `TabController` for the `UserRole.both` case
- `UserRole.poster` → renders `PosterHomeScreen` directly
- `UserRole.provider` → renders `ProviderHomeScreen` directly
- `UserRole.both` → renders a `Scaffold` with `TabBar` ('Find Help' / 'Find Work') + `TabBarView`
- Loading/null user → shows `CircularProgressIndicator`
- Removed old `_EarningsBanner`, `_CategoryGrid`, `_NearbyTasksList` widgets (replaced by role-specific screens)
- Removed unused imports (`category_constants.dart`, `post_task_screen.dart`)
- Files changed: `lib/features/home/presentation/screens/main_shell_screen.dart`
- **Learnings for future iterations:**
  - Do NOT call `controller.dispose()` inside `build()` — only in `dispose()`. Use `??=` to lazily init the TabController.
  - Converting `ConsumerWidget` → `ConsumerStatefulWidget` is needed whenever you need a `TickerProvider` (e.g., for `TabController`)
  - The `HomeScreen` class in `main_shell_screen.dart` is what the router renders for `AppRoutes.home` — it's the entry point for the home tab
---
