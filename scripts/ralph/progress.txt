# Ralph Progress Log
Started: Sat 28 Mar 2026 03:35:43 +08
---

## Codebase Patterns
- Integration tests live in `integration_test/` at project root, using Flutter's `integration_test` SDK package
- Firebase emulator ports: Auth=9099, Firestore=8080, Storage=9199, Functions=5001 (defined in `firebase.json` and mirrored in `integration_test/firebase_emulator_config.dart`)
- Models use Freezed with `fromJson` factories; Firestore deserialization via `XModelExt.fromFirestore(doc)` static extensions
- State management: Riverpod (`flutter_riverpod`), routing: GoRouter with ShellRoute
- Firestore collection names are in `lib/core/constants/app_constants.dart` (e.g. `usersCol`, `tasksCol`)
- User roles: `poster`, `provider`, `both` — stored as string in Firestore
- Task statuses: `open`, `assigned`, `inProgress`, `completed`, `cancelled`, `disputed`
- All 242 Sprint 4 unit/widget tests must remain green
- Dev login navigates to `/auth/role` (role selection) not `/home` — modified in welcome_screen.dart
- Router redirect exempts `/auth/role` and `/auth/profile-setup` so logged-in users can complete onboarding
- Integration tests pump `ProviderScope(child: NeighbourGoApp())` directly — no need to call `main()`
- For splash screen timing: use `pump(Duration(seconds: 3))` then `pumpAndSettle()` to wait for 2s delay + navigation
- Use `find.widgetWithText(TextFormField, 'hint text')` to target specific form fields by their hint text
- For dropdown selection: tap the `DropdownButtonFormField<String>`, wait for overlay, then `find.text('Option').last` to tap the overlay item
- Scroll before interacting with off-screen widgets: `tester.drag(find.byType(SingleChildScrollView), Offset(0, -300))`
- Bids are a Firestore subcollection: `tasks/{taskId}/bids/{bidId}` — use `firestore.collection('tasks').doc(taskId).collection('bids')` to query
- Multi-user integration tests: create both users in setUpAll, use `signOutTestUser()` / `signInTestUser()` + re-pump the app widget to switch users

---

## 2026-03-28 - US-028
- Created integration test infrastructure with Firebase Emulator support
- Files created:
  - `integration_test/firebase_emulator_config.dart` — emulator host/port constants
  - `integration_test/test_helpers.dart` — initializeTestApp(), signInTestUser(), signOutTestUser(), cleanupEmulatorData(), pumpAndSettle()
  - `integration_test/test_data.dart` — factory methods for test users (poster, provider, both) and tasks (cleaning, tutoring) with Singapore data; Firestore seeding helpers
  - `integration_test/app_test.dart` — main entry point with smoke tests for emulator connectivity
- Files modified:
  - `pubspec.yaml` — added `integration_test` SDK dependency
  - `ios/Runner/Info.plist` — added `NSAllowsLocalNetworking` for localhost emulator connections
- **Learnings for future iterations:**
  - `flutter analyze` has 0 errors but ~110 pre-existing warnings/infos in Sprint 4 test files (sealed class subtyping, prefer_const) — these are not blockers
  - `signInTestUser()` supports both anonymous and email/password auth for multi-user test scenarios
  - Test data factories return `Map<String, dynamic>` (not model objects) for direct Firestore seeding — this avoids Freezed serialization quirks with `FieldValue.serverTimestamp()`
  - iOS Info.plist needs `NSAllowsLocalNetworking` under `NSAppTransportSecurity` for simulator-to-localhost emulator connections
---

## 2026-03-28 - US-029
- Implemented integration test for Dev Login & Onboarding Flow
- Files created:
  - `integration_test/auth_flow_test.dart` — tests full flow: splash → welcome → dev login → role selection → profile setup → home
- Files modified:
  - `lib/features/auth/presentation/screens/welcome_screen.dart` — dev login now navigates to `/auth/role` (role selection) instead of `/home`
  - `lib/core/router/app_router.dart` — router redirect exempts `/auth/role` and `/auth/profile-setup` so authenticated users can complete onboarding
  - `scripts/ralph/prd.json` — marked US-029 as passes: true
- **Learnings for future iterations:**
  - The dev login `_devLogin()` creates a user doc with `role: both` via `createOrUpdateUser()` — this doc must exist before `ProfileSetupScreen._save()` calls `profileRepo.updateProfile()` (which uses Firestore `.update()`, not `.set()`)
  - `createOrUpdateUser()` only updates `lastActiveAt` for existing docs — it does NOT update role or other fields. Role selection's `_confirm()` effectively does nothing for existing docs.
  - `currentUserProvider` has a fallback: if no Firestore doc exists, it synthesizes a minimal `UserModel` from the auth user (role=both, displayName='User')
  - `PosterHomeScreen` greeting uses `displayName.split(' ').first` — so 'Test User' becomes 'Hi, Test! 👋'
  - The `NeighbourGoApp` widget can be pumped directly with `ProviderScope` wrapper — Stripe/Crashlytics init in `main()` is not needed for integration tests
---

## 2026-03-28 - US-030
- Implemented integration test for Post Task End-to-End (5-step flow)
- Files created:
  - `integration_test/post_task_test.dart` — tests full flow: home → post FAB → category → description → location → budget → review → post → verify Firestore → verify home active tasks
- **Learnings for future iterations:**
  - PostTaskScreen uses 5 steps (index 0-4): Category, Description, Location, Budget, Review
  - Category cards display as "emoji label" (e.g. "Home Cleaning") — use the label text to find and tap
  - Budget field has prefixText "S$ " — find it with `find.widgetWithText(TextFormField, 'S\$ ').first`
  - Step 5 (Review) button says "Post Task" (not "Next") — calls `_submit()` directly with no form validation
  - After successful post, app shows snackbar "Task posted! Providers will bid shortly." and navigates to `/tasks/$id` (TaskDetailScreen)
  - `createTask()` generates a UUID for the task ID, uploads photos to Storage, then writes to Firestore with timestamps
  - Pre-seeded user with email/password auth works well for tests that need the user already logged in
  - The test handles the case where app may land on Welcome/Role/Profile screens if the user session isn't preserved
---

## 2026-03-28 - US-031
- Implemented integration test for Task Discovery & Filtering
- Files created:
  - `integration_test/task_discovery_test.dart` — tests full flow: navigate to Discover tab → verify all 5 seeded tasks visible → filter by Home Cleaning (2 results) → filter by Tutoring (2 results) → clear filter with All → tap task card → verify TaskDetailScreen → navigate back
- **Learnings for future iterations:**
  - Filter chips use custom `_FilterChip` widgets (not Material Chip) — find them by label text like `find.text('Home Cleaning').first` since the same text appears in card category badges
  - Tapping the same category chip again deselects it (toggle behavior), but tapping a different chip switches to that filter directly
  - The "All" chip text is just `'All'` — tapping it passes `null` to the category provider to clear the filter
  - Task cards display: category badge (emoji + label), urgency, title, neighbourhood/location, time, budget, and bid count
  - TaskDetailScreen AppBar shows `task.title` — useful for verifying navigation to detail
  - When seeding multiple tasks of the same type, spread the base factory and override specific fields: `{...testCleaningTask(...), 'title': 'Custom Title'}`
  - Scroll management matters: after filtering tests, scroll back to top before tapping a card to avoid off-screen tap failures
---

## 2026-03-28 - US-032
- Implemented integration test for Bidding Flow — Provider Submits, Poster Accepts
- Files created:
  - `integration_test/bidding_flow_test.dart` — tests full flow: provider signs in → discovers task → submits bid → verify Firestore → poster signs in → sees bid → accepts → verify Firestore status updates
- **Learnings for future iterations:**
  - Bids are stored as subcollection: `tasks/{taskId}/bids/{bidId}`, NOT as a top-level `bids` collection
  - `submitBid()` increments `bidCount` on the task doc via `FieldValue.increment(1)`
  - `acceptBid()` uses a batch write: sets accepted bid to 'accepted', all other pending bids to 'rejected', and updates task to status='assigned' with assignedProviderId/Name
  - After accepting a bid, the app pushes to checkout route (`/checkout/:taskId`) — the test verifies Firestore state without following that navigation
  - Multi-user tests: create both users in `setUpAll`, sign in/out between them; restart the app widget with `pumpWidget` after switching users
  - SubmitBidSheet amount field has hint '0.00' and prefix 'S$ ', proposal field has min 20 chars validation
  - The `BidListSection` shows "Bids received (N)" as title — useful for asserting bid count in poster view
  - Provider's display name on bid card comes from `providerName` field stored in the bid document, sourced from `user.displayName` at submission time
---
