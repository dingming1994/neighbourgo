# Ralph Progress Log
Started: Wed  1 Apr 2026 23:43:59 +08
---

## Codebase Patterns
- EmptyState widget lives at `lib/core/widgets/empty_state.dart`; also re-exported from `loading_overlay.dart` for backward compat
- Use `CachedNetworkImage` (from `cached_network_image` package) instead of `Image.network()` — always provide `placeholder` and `errorWidget`
- When creating TextEditingControllers dynamically (e.g. in a Map), dispose them when removed from the map, not just in `dispose()`
- Profile screens are at `lib/features/profile/presentation/screens/`
- `AppCategories.all` and `AppCategories.getById()` in `category_constants.dart` for category lookups

---

## 2026-04-01 - US-059
- Created shared `EmptyState` widget at `lib/core/widgets/empty_state.dart` (extracted from `photo_gallery_screen.dart`)
- Updated `loading_overlay.dart` to re-export `empty_state.dart` so `public_profile_screen.dart` (which imports `loading_overlay.dart`) continues to work
- Replaced `Image.network()` with `CachedNetworkImage` in `photo_gallery_screen.dart` with placeholder and error widget
- Fixed empty caption overlay: changed condition to `caption != null && caption!.isNotEmpty`
- Fixed rate controller memory leak in `edit_profile_screen.dart`: controllers for deselected categories are now disposed immediately in the `onSelected` callback
- Files changed:
  - `lib/core/widgets/empty_state.dart` (new)
  - `lib/core/widgets/loading_overlay.dart` (removed EmptyState, added re-export)
  - `lib/features/profile/presentation/screens/photo_gallery_screen.dart`
  - `lib/features/profile/presentation/screens/edit_profile_screen.dart`
- **Learnings for future iterations:**
  - `loading_overlay.dart` already had an EmptyState with an `action` parameter — check existing widgets before creating new ones
  - `public_profile_screen.dart` gets EmptyState via `loading_overlay.dart` import, not a direct import
  - The `_rateControllers` map in `edit_profile_screen.dart` has controllers created in `build()` via `putIfAbsent` — disposal needs to happen both in `dispose()` and when categories are deselected
---
