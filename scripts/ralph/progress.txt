# Ralph Progress Log
Started: Tue 31 Mar 2026 20:41:10 +08
---

## Codebase Patterns
- Cloud Functions use `admin.firestore.Timestamp.now()` (not `Timestamp.now()`) for timestamps
- Cloud Functions use `admin.firestore.FieldValue.increment()` for atomic counter updates
- All Cloud Functions are deployed to `asia-southeast1` region
- Stripe config loaded from `functions.config().stripe` with env var fallback
- Functions build: `cd functions && npm run build` (no test script exists)
- Flutter quality checks: `flutter analyze` (0 errors required) + `flutter test`
- Pre-existing test failure in `test/widgets/auth_screens_test.dart` — "Dev Login (Simulator)" text not found

---

## 2026-03-31 20:45 +08 - US-041
- Audited all 8 Cloud Functions in `functions/src/index.ts`
- **Result:** All functions already use correct `admin.firestore.Timestamp.now()` pattern — no fix needed
- Verified: `npm run build` compiles successfully, compiled `lib/index.js` matches source
- Verified: `flutter analyze` shows 0 errors (only deprecation warnings)
- Files reviewed: `functions/src/index.ts`, `functions/lib/index.js`, `functions/package.json`
- **Learnings for future iterations:**
  - The Cloud Functions code was already correct from the initial commit — PRD described a hypothetical issue
  - No test script exists in functions/package.json — only build verification is possible
  - Pre-existing test failure in auth_screens_test.dart (unrelated to functions)
---
