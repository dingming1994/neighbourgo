# Ralph Progress Log
Started: Fri  3 Apr 2026 00:50:32 +08
---

## Codebase Patterns
- Freezed models live in `features/<name>/data/models/` with `part` files for `.freezed.dart` and `.g.dart`
- Repositories follow `features/<name>/data/repositories/` pattern with `Provider<Repo>` at bottom
- Firestore Timestamp → DateTime conversion done in `fromFirestore` extension methods
- Routes defined in `AppRoutes` class in `app_constants.dart`, wired in `app_router.dart`
- Screens use `ConsumerWidget` / `ConsumerStatefulWidget` for Riverpod access
- DiscoverScreen uses `DiscoverSegment` enum with embedded views (`embedded: true` flag)
- Provider directory category filter uses `StateProvider.autoDispose<String?>` pattern
- Always put `/create` routes before `/:id` routes in GoRouter to avoid wildcard matches
- Pre-existing test failures (4): profile_chat_nav_test EditProfileScreen tests + task_repository_test watchMyPostedTasks
- Client-side search: use `discoverSearchQueryProvider` (shared StateProvider) — embedded screens read it only when `embedded: true`
- Querying subcollections across parents requires `collectionGroup()` + COLLECTION_GROUP index in firestore.indexes.json
- Profile menu items can be role-gated: `if (user.isProvider)` before `_MenuItem`

---

## 2026-04-03 — US-070: Provider Service Listings — Publish & Browse Services
- Implemented full ServiceListing feature: model, repository, create screen, detail screen, card widget, browse screen
- Files created:
  - `lib/features/services/data/models/service_listing_model.dart` (+ .freezed.dart, .g.dart)
  - `lib/features/services/data/repositories/service_listing_repository.dart`
  - `lib/features/services/presentation/screens/create_service_screen.dart`
  - `lib/features/services/presentation/screens/service_detail_screen.dart`
  - `lib/features/services/presentation/screens/service_listings_screen.dart`
  - `lib/features/services/presentation/widgets/service_card.dart`
  - `test/models/service_listing_model_test.dart`
  - `integration_test/service_listing_test.dart`
- Files modified:
  - `lib/core/constants/app_constants.dart` — added collection, storage path, routes
  - `lib/core/router/app_router.dart` — added service routes + imports
  - `lib/features/home/presentation/screens/provider_home_screen.dart` — added My Services section
  - `lib/features/discover/presentation/screens/discover_screen.dart` — added Services segment
  - `firestore.rules` — added service_listings collection rules
- **Learnings for future iterations:**
  - The DiscoverScreen uses a 3-way enum toggle (tasks/providers/services) — add new segments by extending `DiscoverSegment`
  - Hire button on ServiceDetailScreen navigates to PostTaskScreen with `directHireProviderId`, `directHireProviderName`, and `preSelectedCategory` extras
  - Service listings use `service_listings` Firestore collection (top-level, not nested)
  - Build runner may report "0 outputs" if generated files already exist from a prior run
---

## 2026-04-03 — US-071: Global Search — Search Tasks and Providers
- Added search bar at top of DiscoverScreen with 300ms debounce, clear button
- Client-side filtering on all three tabs: tasks (title/description), providers (displayName/headline/serviceCategories), services (title/description/providerName)
- Shared `discoverSearchQueryProvider` StateProvider read by all embedded screens
- Files modified:
  - `lib/features/discover/presentation/screens/discover_screen.dart` — added search provider, search bar, converted to StatefulWidget for debounce timer
  - `lib/features/tasks/presentation/screens/task_list_screen.dart` — reads search query when embedded, filters tasks client-side
  - `lib/features/providers/presentation/screens/provider_directory_screen.dart` — reads search query when embedded, filters providers client-side
  - `lib/features/services/presentation/screens/service_listings_screen.dart` — reads search query when embedded, filters services client-side
- **Learnings for future iterations:**
  - Search filtering is done client-side on the already-loaded list data — no extra Firestore queries needed
  - The `discoverSearchQueryProvider` is shared across all Discover tabs — new tabs should read it when `embedded: true`
  - Embedded screens check `widget.embedded` (or `embedded`) before watching the search provider to avoid reading it when used standalone
---

## 2026-04-03 — US-072: My Tasks & My Bids History Pages
- Created `my_tasks_screen.dart` with 3 tabs (Active, Completed, Cancelled) replacing `_MyTasksTab` placeholder
- Created `my_bids_screen.dart` with 3 tabs (Pending, Accepted, Rejected) for provider bid history
- Added `watchMyCancelledPostTasks` to TaskRepository for cancelled tasks query
- Added `watchMyBids` collection group query to BidRepository for provider's bids across all tasks
- Added `myBids` route (`/bids/mine`) to AppRoutes and GoRouter
- Added "My Bids" menu item to ProfileScreen (visible only for providers via `user.isProvider`)
- Added Firestore collection group index for `bids` (providerId + createdAt)
- Files created:
  - `lib/features/tasks/presentation/screens/my_tasks_screen.dart`
  - `lib/features/bids/presentation/screens/my_bids_screen.dart`
- Files modified:
  - `lib/features/tasks/data/repositories/task_repository.dart` — added `watchMyCancelledPostTasks`
  - `lib/features/bids/data/repositories/bid_repository.dart` — added `watchMyBids` (collection group)
  - `lib/core/constants/app_constants.dart` — added `myBids` route
  - `lib/core/router/app_router.dart` — replaced `_MyTasksTab` stub, added myBids route, imported new screens
  - `lib/features/profile/presentation/screens/profile_screen.dart` — added My Bids menu item
  - `firestore.indexes.json` — added bids collection group index
- **Learnings for future iterations:**
  - Bids are subcollections under tasks (`tasks/{taskId}/bids`), so querying "all bids by a provider" requires `collectionGroup('bids')` + a COLLECTION_GROUP index in firestore.indexes.json
  - Task titles for bids must be fetched separately since BidModel doesn't store task title — use `FutureProvider.family` with `watchTask(taskId)` for on-demand loading
  - The `_MyTasksTab` stub in app_router.dart is now removed; MyTasksScreen is imported directly
  - ProfileScreen menu items can be conditionally shown with `if (user.isProvider)` / `if (user.isPoster)` checks
---

## 2026-04-03 — US-073: Enhanced Filtering — Price, Rating, Sort
- Added filter bottom sheet on TaskListScreen: budget range slider (S$0–S$500), neighbourhood dropdown, sort (newest/price low-high/price high-low)
- Added filter bottom sheet on ProviderDirectoryScreen: min rating slider (0–5), neighbourhood dropdown, sort (highest rated/most reviews/newest)
- Filter icon with active count badge on both category filter bars
- Clear all button resets to defaults
- Client-side filtering & sorting applied on already-loaded data (same pattern as search)
- Files created:
  - `lib/features/tasks/presentation/widgets/task_filter_sheet.dart`
  - `lib/features/providers/presentation/widgets/provider_filter_sheet.dart`
- Files modified:
  - `lib/features/tasks/domain/providers/task_list_provider.dart` — added TaskFilterState, TaskSortOption, taskFilterProvider
  - `lib/features/providers/domain/providers/provider_list_provider.dart` — added ProviderFilterState, ProviderSortOption, providerFilterProvider
  - `lib/features/tasks/presentation/screens/task_list_screen.dart` — integrated filter icon, _applyFiltersAndSort, _FilterIconButton
  - `lib/features/providers/presentation/screens/provider_directory_screen.dart` — integrated filter icon, _applyFiltersAndSort, _FilterIconButton
- **Learnings for future iterations:**
  - Filter state is kept in StateProvider (not autoDispose) so it persists across tab switches in Discover
  - The _CategoryFilterBar was updated to accept optional filterCount + onFilterTap for the tune icon — same pattern in both screens
  - Neighbourhood filter uses case-insensitive `contains` match since task/provider data may have varying formats
  - Singapore neighbourhood list is hardcoded in both filter sheets — could be extracted to a shared constant if needed
---

## 2026-04-03 — US-074: UI Polish — Design Consistency, Animations, Empty States
- Added shimmer loading skeletons to ChatListScreen, NotificationListScreen, MyTasksScreen, MyBidsScreen (all previously used CircularProgressIndicator)
- Replaced ChatListScreen icon-based empty state with emoji (💬) + consistent text styling
- Enhanced NotificationListScreen empty state with larger emoji (🔔 at 56px) + subtitle
- Fixed NotificationListScreen AppBar to match standard pattern (bgCard, elevation 0, fontWeight w700)
- Added pull-to-refresh (RefreshIndicator) to ChatListScreen, NotificationListScreen, MyTasksScreen, MyBidsScreen, ServiceListingsScreen
- Fixed text overflow in _RecommendedTaskCard and _ProviderTaskCard neighbourhood text (added Expanded + maxLines + ellipsis)
- Fixed text overflow in _JobOfferCard posterName and neighbourhood text
- Updated test to match new emoji empty state in ChatListScreen
- Files modified:
  - `lib/features/chat/presentation/screens/chat_list_screen.dart` — shimmer, emoji empty state, pull-to-refresh
  - `lib/features/notifications/presentation/screens/notification_list_screen.dart` — shimmer, AppBar fix, empty state polish, pull-to-refresh
  - `lib/features/tasks/presentation/screens/my_tasks_screen.dart` — shimmer, pull-to-refresh
  - `lib/features/bids/presentation/screens/my_bids_screen.dart` — shimmer, pull-to-refresh
  - `lib/features/services/presentation/screens/service_listings_screen.dart` — pull-to-refresh
  - `lib/features/home/presentation/screens/provider_home_screen.dart` — text overflow fixes
  - `lib/features/home/presentation/widgets/job_offers_section.dart` — text overflow fixes
  - `test/widgets/profile_chat_nav_test.dart` — updated empty state assertion
- **Learnings for future iterations:**
  - Shimmer pattern for list tiles: Row with circle (avatar) + Column of rectangles; for card lists: single Container with card height
  - Pull-to-refresh with StreamProviders: use `ref.invalidate(provider)` inside `onRefresh` callback
  - Always add `physics: AlwaysScrollableScrollPhysics()` when wrapping ListView in RefreshIndicator so it works even with short lists
  - Pre-existing test failures (4) unchanged: EditProfileScreen tests + task_repository_test watchMyPostedTasks
---
