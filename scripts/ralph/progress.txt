# Ralph Progress Log
Started: Fri  3 Apr 2026 14:19:53 +08
---

## Codebase Patterns
- Delete account flow uses a two-step confirmation dialog pattern in settings_screen.dart
- AppColors (from core/theme/app_theme.dart) is the standard way to reference colors
- State management uses Riverpod (ConsumerStatefulWidget / ref.read)
- signOutProvider handles sign-out after account deletion
- Pre-existing warnings/info in flutter analyze (~169 issues) — all are non-error (unused imports, const suggestions, deprecated members)
- Use `kDebugMode` from `flutter/foundation.dart` to gate debug-only UI; use `if (cond) ...[widgets]` pattern in Column children
- Reusable `ErrorState` widget exists at `core/widgets/error_state.dart` — use it for all error branches instead of inline `Text('Error: $e')`
- Use `ref.invalidate(provider)` in Riverpod error retry callbacks to re-fetch data
- ServiceDetailScreen now uses a `serviceDetailProvider` StreamProvider (converted from raw StreamBuilder) for consistency
- `CreateServiceScreen` supports both create and edit mode via optional `existingListing` param — reuse this pattern for other create/edit screens
- Pass complex model objects to GoRouter routes via `state.extra` (not URL params)
---

## 2026-04-03 - US-101
- Replaced the second confirmation dialog in `_confirmDeleteAccount()` with a new `_DeleteConfirmationDialog` stateful widget
- Added a TextField with hint 'Type DELETE to confirm' that validates input case-sensitively
- Delete button is disabled (null onPressed) until text equals 'DELETE'
- Added a loading spinner (CircularProgressIndicator) shown during the delete operation
- Cancel button pops dialog with `false`, no side effects
- Files changed: `lib/features/settings/presentation/screens/settings_screen.dart`
- **Learnings for future iterations:**
  - The settings screen had an unused import (`auth_repository.dart`) — pre-existing, don't touch
  - `withValues(alpha:)` is the non-deprecated way to set opacity on colors in Flutter
  - Dialog uses `barrierDismissible: false` to prevent accidental dismissal
  - No existing tests for settings_screen — manual browser verification needed for UI changes
---

## 2026-04-03 - US-102
- Wrapped Dev Login button and its SizedBox spacer in `if (kDebugMode) ...[...]` so the button is completely absent from the widget tree in release builds
- Added `import 'package:flutter/foundation.dart'` for `kDebugMode`
- Files changed: `lib/features/auth/presentation/screens/welcome_screen.dart`
- **Learnings for future iterations:**
  - `kDebugMode` from `flutter/foundation.dart` is the standard way to gate debug-only UI in Flutter
  - The `_devLogin` method remains in the file but is unreachable in release — Dart tree-shaking handles dead code
  - Using `if (condition) ...[widgets]` in Column children is the idiomatic way to conditionally include multiple widgets
  - No existing tests for welcome_screen — manual browser verification needed
---

## 2026-04-03 - US-103
- Reused existing `ErrorState` widget from `core/widgets/error_state.dart` (has error icon, user-friendly message, and "Try Again" button)
- Replaced inline `Text('Error: $e')` with `ErrorState(onRetry: ...)` in 4 screens:
  - `TaskDetailScreen` — retry calls `ref.invalidate(taskDetailProvider(taskId))`
  - `PublicProfileScreen` — retry calls `ref.invalidate(publicProfileProvider(userId))` for main profile; also fixed reviews error state with `ref.invalidate(userReviewsProvider(userId))`
  - `ServiceDetailScreen` — converted from raw `StreamBuilder` to `StreamProvider.family` (`serviceDetailProvider`) for consistency with other screens; retry calls `ref.invalidate(serviceDetailProvider(listingId))`
  - `ChatThreadScreen` — retry calls `ref.invalidate(messagesStreamProvider(widget.chatId))`
- Files changed:
  - `lib/features/tasks/presentation/screens/task_detail_screen.dart`
  - `lib/features/profile/presentation/screens/public_profile_screen.dart`
  - `lib/features/services/presentation/screens/service_detail_screen.dart`
  - `lib/features/chat/presentation/screens/chat_thread_screen.dart`
- **Learnings for future iterations:**
  - `ErrorState` widget already existed in `core/widgets/` — always check for existing reusable widgets before creating new ones
  - Converting `StreamBuilder` to `StreamProvider.family` enables `ref.invalidate()` for retry, which is cleaner than rebuilding with setState
  - PublicProfileScreen has two separate error states (profile + reviews) — both need updating when fixing error handling
  - Manual browser verification needed — no tests cover these error states
---

## 2026-04-03 - US-104
- Added `LinearProgressIndicator` below search bar in `discover_screen.dart` that shows during 300ms debounce period (tracks `_isDebouncing` state)
- Added `_SearchEmptyView` widget to all 3 tab screens showing "No results for 'query'" with `search_off_rounded` icon and suggestion to adjust filters
- Added `_SearchResultCount` widget showing "N tasks/providers/services found" between category filter bar and list when search has results
- Clearing search resets to default unfiltered view (already worked, debounce cancel added for cleanliness)
- Files changed:
  - `lib/features/discover/presentation/screens/discover_screen.dart`
  - `lib/features/tasks/presentation/screens/task_list_screen.dart`
  - `lib/features/providers/presentation/screens/provider_directory_screen.dart`
  - `lib/features/services/presentation/screens/service_listings_screen.dart`
- **Learnings for future iterations:**
  - Search is purely client-side (filters already-loaded items) — no server round-trip, so "loading" is really just the debounce delay
  - Each tab screen (Tasks, Providers, Services) has its own `_EmptyView` and `_buildBody` — changes must be applied to all three separately
  - `ServiceListingsScreen` uses `StreamProvider` + `.when()` pattern (different from Tasks/Providers which use `StateNotifier`), so result count widget is inserted differently (inside the `data:` callback as a wrapping `Column`)
  - Manual browser verification needed — no tests cover search UX states
---

## 2026-04-03 - US-105
- Added Edit (pencil) and Delete (trash) icon buttons to `ServiceDetailScreen` SliverAppBar, visible only when `currentUser.uid == listing.providerId`
- Extended `CreateServiceScreen` with optional `existingListing` parameter for edit mode — pre-populates all form fields (category, title, description, pricing, availability, photos)
- Existing photos shown as network images with remove buttons; new photos can be added alongside them (max 6 total)
- Added `uploadPhotos()` helper to `ServiceListingRepository` to handle photo uploads separately from creation
- Edit mode calls `updateListing()` with field map and shows "Service listing updated!" snackbar
- Delete shows confirmation dialog; confirming calls `deleteListing()`, shows snackbar, and pops back
- Non-owners see no edit/delete buttons (controlled by `isOwnListing` check)
- Added `editService` route (`/services/:listingId/edit`) to `AppRoutes` and `app_router.dart`; passes `ServiceListingModel` via `extra`
- Files changed:
  - `lib/features/services/presentation/screens/service_detail_screen.dart`
  - `lib/features/services/presentation/screens/create_service_screen.dart`
  - `lib/features/services/data/repositories/service_listing_repository.dart`
  - `lib/core/constants/app_constants.dart`
  - `lib/core/router/app_router.dart`
- **Learnings for future iterations:**
  - `CreateServiceScreen` now supports both create and edit modes via optional `existingListing` param — reuse this pattern for other screens
  - GoRouter `extra` is the cleanest way to pass complex objects (like models) to routes without serialization
  - Repository already had `updateListing` and `deleteListing` methods — always check existing repo methods before adding new ones
  - Photo management in edit mode needs two lists: `_existingPhotoUrls` (network URLs to keep) and `_photos` (new local files to upload)
  - Manual browser verification needed — no tests cover service edit/delete flow
---

## 2026-04-03 - US-106
- Upgraded email validation in `email_auth_screen.dart` from basic `@` and `.` check to proper regex that rejects `user@`, `user@.com`, etc.
- Changed bid amount validator in `submit_bid_sheet.dart`: removed hard error at 2× budget, added warning `Chip` with orange styling that appears dynamically via `onChanged` — still allows submission
- Added `helperText: 'Min S$5'` to the minimum budget field in `post_task_screen.dart`
- Converted rate `TextField` to `TextFormField` in `edit_profile_screen.dart` with `FilteringTextInputFormatter` for decimals and validator enforcing S$1–S$500 range
- Added `import 'package:flutter/services.dart'` to edit_profile_screen for `FilteringTextInputFormatter`
- Files changed:
  - `lib/features/auth/presentation/screens/email_auth_screen.dart`
  - `lib/features/bids/presentation/widgets/submit_bid_sheet.dart`
  - `lib/features/tasks/presentation/screens/post_task_screen.dart`
  - `lib/features/profile/presentation/screens/edit_profile_screen.dart`
- **Learnings for future iterations:**
  - Rate fields in edit_profile_screen were plain `TextField` (not `TextFormField`) — always check widget type when adding validators
  - `FilteringTextInputFormatter.allow(RegExp(r'^\d+\.?\d{0,2}'))` is the standard pattern for decimal currency input across the codebase
  - Warning chips (non-blocking) are preferred over hard validation errors for soft limits — use `onChanged` + `setState` for real-time warning display
  - Manual browser verification needed — no tests cover form validation edge cases
---
