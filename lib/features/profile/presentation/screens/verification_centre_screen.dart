import 'package:flutter/material.dart';

class VerificationCentreScreen extends StatelessWidget {
  const VerificationCentreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Verification Centre')),
      body: const Center(child: Text('Verification Centre – coming soon')),
    );
  }
}
